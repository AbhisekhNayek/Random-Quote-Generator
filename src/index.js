// Importing React and ReactDOM modules
import React from 'react';
import ReactDOM from 'react-dom';

// Importing the main App component from './App'
import App from './App';

// Rendering the App component to the root HTML element
ReactDOM.render(
  <App />,  // The App component is rendered here
  document.getElementById('root')  // Rendering it to the HTML element with id 'root'
);
